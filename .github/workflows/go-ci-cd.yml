name: CI/CD Pipeline

on: 
  push:
    branches: ["main", "feature*"]
    tags: ['v*']
  pull_request:
    branches: ["main", "feature*"]
    
env:
  GO_VERSION: '1.23.2'
  ALPINE_VERSION: '3.20'
  PORT: '80'
  LINTER_VERS: 'v1.60.1'
  UBUNTU_VERS: '22.04'
  CI_VERS: 'v0.0.1'

jobs:
  ci:
    uses: github.com/Mark-Grigorev/central-ci-cd/.github/wokflows/go/go-ci-comlete.yml@${{ env.CI_VERS }}
    with:
      go-version: ${{ env.GO_VERSION }}
      linter-version: ${{ env.LINTER_VERS }}
      ubuntu-version: ${{ env.UBUNTU_VERS }}